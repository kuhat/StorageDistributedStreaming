package utils;

public enum ComponentTitle {
    RECONFIG_NOTIFICTATION_RECEIVER
            {
               public String toString()
               {
                   return "Re-config notification receiver";
               }
            }
}